{
  "name": "transclude-example-atlas",
  "private": true,
  "type": "module",
  "description": "A reader for the AT Protocol network. Every record is a fragment other sites can embed.",
  "scripts": {
    "dev": "transclude-dev",
    "build": "transclude-build",
    "start": "transclude-serve",
    "check": "transclude-check",
    "test": "node --test \"test/**/*.test.js\"",
    "preview": "npm run build && npm start",
    "start:worker": "wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@transclude/core": "file:../.."
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.1.5"
  }
}
