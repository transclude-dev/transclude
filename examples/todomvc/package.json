{
  "name": "transclude-example-todomvc",
  "private": true,
  "type": "module",
  "description": "TodoMVC with nothing but forms. No client JavaScript at all.",
  "scripts": {
    "dev": "transclude-dev",
    "build": "transclude-build",
    "start": "transclude-serve",
    "check": "transclude-check",
    "test": "node --test \"test/**/*.test.js\"",
    "preview": "npm run build && npm start",
    "start:worker": "npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "@transclude/core": "file:../.."
  },
  "devDependencies": {
    "typescript": "^7.0",
    "vite": "^8.1.5"
  }
}
